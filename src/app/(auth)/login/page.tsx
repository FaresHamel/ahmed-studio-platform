import LoginContainer from "@/containers/auth/LoginContainer";

export const metadata = {
  title: "Login | Ahmed Studio",
  description: "Sign in to access your account"
};

export default function LoginPage() {
  return <LoginContainer />;
}
